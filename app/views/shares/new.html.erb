<%= form_for @share, :html => { :multipart => true } do |f| %>
	<p>
		<%= f.label :title %><br>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :picture %>
  	<%= f.file_field :picture %>
	</p>

	<p>
		<%= f.label :clip_list, 'Clips' %>
		<%= f.text_field :clip_list %>

	<p>
		<%= f.submit 'Create share' %>
	</p>
<% end %>